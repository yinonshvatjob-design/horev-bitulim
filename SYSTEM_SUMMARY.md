# סיכום מערכת ביטול ארוחות — מוסדות חורב ירושלים (Bitulim Platform)

קובץ זה נוצר כסיכום וגיבוי של המבנה, הארכיטקטורה והפיצ'רים של פלטפורמת ביטול הארוחות. **הארכיטקטורה, הכתובת והעקרונות זהים ב-100% לפרויקט ניהול המלאי (school-inventory)**.

---

## 🌐 כתובת הגישה המוסדית הקבועה מרחוק
- 🔗 **כתובת הגישה המוסדית הקבועה ב-Cloudflare (זהה במבנה למלאי):**
  👉 **`https://bitulim.horev.org.il`**

---

## 1. ארכיטקטורה כללית (זהה לפרויקט ניהול המלאי)
- **צד שרת (API Engine):** בנוי ב-Node.js / Express עם אימות JWT, Nodemailer ושירות דיוור חי. מקושר למסד נתונים **PostgreSQL v15**.
- **צד לקוח (Web Client):** בנוי ב-HTML5, Vanilla CSS3 מתקדם (HSL, Dark/Light Mode), Google Fonts Rubik & Heebo, ותמיכה מלאה ב-RTL בעברית.
- **מסד נתונים:** **PostgreSQL v15** במכולה מבודדת (`school_bitulim_db_prod`).

---

## 2. מודולים ופיצ'רים מרכזיים
1. **הגשת ביטולים ואכיפת חוק 48 שעות:**
   - בדיקה אוטומטית של מועד האירוע והתראת חסימה אם האירוע מתקיים בתוך פחות מ-48 שעות מראש.
2. **אישור 2 הנחיות חובה מוסדיות:**
   - חובת סימון צ'קבוקס (שמירת קבלות לאסתר/חגי + אחסון אוכל בצוללת).
3. **אישור Custom וסכום החזר ₪ ע"י חגי היקר:**
   - קביעת סכום ההחזר ב-₪ בלבד ע"י חגי היקר, ובחירת הימים/הארוחות שאושרו.
4. **מערכת דיוור דו-כיוונית מלאה מ-bitulim@horev.org.il:**
   - שליחת אימייל התראה לחגי היקר (`chagi@horev.org.il`) ולאסתר (`esters@horev.org.il`) בעת הגשת בקשה.
   - שליחת אימייל עדכון אוטומטי לרכז/ת בעת אישור/דחייה עם סכום ההחזר ב-₪, הערות חגי ו-2 הנחיות החובה.
5. **דוחות וייצוא ל-Excel/CSV:**
   - סינון לפי רכז/ת, סטטוס וטווח תאריכים, עם ייצוא בלחיצת כפתור.
6. **אבטחה וניהול הרשאות (RBAC):**
   - 3 אדמינים (ינון `0542065606`, חגי `0584220463`, אסתר `05455408280`) עם ת"ז + סיסמה.
   - 26 רכזים מורשים עם ת"ז בלבד (ללא שום גישה למסכי הניהול!).

---

## 3. פריסת שרת והרצה (Deployment - Docker & Cloudflare Tunnel)
הארכיטקטורה ערוזה ב-Docker במבנה זהה לפרויקט ניהול המלאי:
- `db`: קונטיינר מסד הנתונים PostgreSQL v15 השומר את הנתונים בווליום קשיח `postgres_bitulim_data_prod`.
- `api`: שרת הבקאנד בפורט `4050`.
- `web`: אתר המערכת בפורט `8080`.
- `cloudflared`: דאמון (Tunnel) של Cloudflare שחוסף את המערכת תחת הדומיין המאובטח `https://bitulim.horev.org.il`.

### הרצה בפרודקשיין:
```bash
docker-compose -f docker-compose.prod.yml up -d --build
```

### יצירת עותק לגיבוי מסד הנתונים (זהה לניהול המלאי):
```bash
powershell ./Backup-Database.ps1
```
